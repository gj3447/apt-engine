"""ooptdd-loop integration for apt-engine.

Trace gates (positive-arrival, Longinus-bound) proving the deep-think fixes emit
real events earned by running real apt_engine code. The loop entry point is
``ooptdd.adapter:run_apt_fix_pipeline``; see ``requirements.yaml``.
"""
